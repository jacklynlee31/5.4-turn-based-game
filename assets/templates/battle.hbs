<center>
	<h1>BATTLE</h1>
</center>

{{#each this as |hero|}}
    <div class="test">
        <img src="images/{{ hero.spriteUrl }}" alt="{{hero.name}}">
    </div>
{{/each}}

{{!-- the photo of whatever the 'chosen' character is --}}

{{!-- the photo of the enemy depending on the round --}}
